rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // App dùng custom username/password (không phải Firebase Auth)
    // nên không dùng request.auth — cho phép đọc/ghi tất cả
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
